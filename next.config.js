/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remover chaves hardcoded - usar apenas variáveis de ambiente
  // As variáveis devem ser definidas no arquivo .env.local
};

module.exports = nextConfig; 